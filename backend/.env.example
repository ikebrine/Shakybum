# Copy to .env and fill in real values. Never commit .env — it's already in .gitignore.

# Copy to .env and fill in real values. Never commit .env — it's already in .gitignore.

# Postgres connection string — for Supabase: Project Settings → Database →
# Connection string → URI (use the "Connection pooling" string, port 6543,
# for serverless/many-short-lived-connections deployments like Vercel
# functions; use the direct connection, port 5432, for a long-running
# server like Railway/Render/Fly).
DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@[YOUR-PROJECT].supabase.co:5432/postgres"

# Separate database for running `npm test` — do NOT point this at your real
# data. Easiest options: a free local Postgres, a second free Supabase
# project, or a Neon/Railway free-tier Postgres used only for CI. Falls
# back to DATABASE_URL if unset, which is fine for solo local dev but risky
# in any shared/CI environment — see README "Running tests".
TEST_DATABASE_URL="postgresql://postgres:password@localhost:5432/shakybum_test"

# JWT signing secret — generate a real random one: `openssl rand -hex 32`
JWT_SECRET="change-me-to-a-random-64-char-hex-string"
JWT_EXPIRES_IN="7d"

# Paystack (https://dashboard.paystack.com/#/settings/developer)
PAYSTACK_SECRET_KEY="sk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
PAYSTACK_PUBLIC_KEY="pk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
# Leave unset in production — only used to point at scripts/mock-paystack-server.js
# for local testing without hitting the real Paystack API.
# PAYSTACK_BASE_URL="http://localhost:5555"
# Set this in the Paystack dashboard webhook config to point at:
#   https://your-deployed-backend.com/api/webhooks/paystack
# Paystack signs webhook payloads with your secret key (see lib/paystack.js) —
# there's no separate webhook secret to configure.

PORT=4000
CORS_ORIGIN="http://localhost:5173"

# Platform's cut of every contact-reveal / Bum-session payment (0.30 = 30%)
PLATFORM_CUT=0.30
